ok, everything is good but one change is to be done, the thing what i observed about detection rateand false alarm rate is, for almost every device the detection and false alarm rate are almost equal, average detection rate percentage came out to be around 80% (which is significantly good)  and false alarm came out to be 75%(which is bad actually), but i think both percentage being closer is due to having different denominators in calculation of detection and false alarm rates. so what i suggest is, instead of focuing on pecentages of detection and false alarm rate, focus on the absolute quantity of cases, like True positive (detection) and False positive (false alarm) , so like a confusion matrix thing, so get the actual number of total instances where the actual ground truth and model predict a charge event and acutal number of instances of false alarm for each device and do some kind of averaging of it across all devices etc ec, so u dont need to change plots or much of code, only instead of "charging_schedule_detection_plot.png — detection rate / false alarm rate per device" plot now i need the absolute quantity of true and false positive value kind of plot of all devices and some kind of average across all devices kind of thing too. So give the whole updated code. And more thing i forgot to tell about, is it necessary to keep threshold conditionon both MIN_HOURLY_RISE(which is on dsocdt parameter) and MIN_CHARGE_RISE(which is on SOC parameter), i think u can just keep condition based on only SOC to fetch the acutal charging events and predicted charging events (like in actual data in wherever hour instant increase in SOC is more than or equal to 40% consider that as charging event hour and similry for predicted side too)
