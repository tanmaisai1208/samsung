ok, i have taken ur code for running the xgboost_charging_schedule.py code, i kept it for runninng, but as it runs i want to do one more task. lets once again go back to final_validation.py part. thing is, the 12 new feature engineered features are used in charge_scheduling part of task, the final_validation task uses the older way only (24 dscodt, 24 soc), i want to know what if the same 12 feature kind of framework is used for final_validation task, so i pasted the current code of final_validation.py, u change the code accordingly (but remember keep the training window and testing window pipeline the same : day1-14 is train and do test on day15, day 2-16 is train for day 16 prediction etc, just change the features train data will contain and passed to model for training as those 12 features)

