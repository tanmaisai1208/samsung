overall_by_value = (results_df.groupby("param_value")["mean_accuracy"]
                                  .mean().reset_index())
    if overall_by_value["mean_accuracy"].isna().all():
        print("No valid results to determine best value. Exiting.")
        raise SystemExit
    best_row = overall_by_value.loc[overall_by_value["mean_accuracy"].idxmax(skipna=True)]
